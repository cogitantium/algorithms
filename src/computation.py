from src.algorithms.computation.iterative_factorial import iterative_factorial
from src.algorithms.computation.recursive_factorial import recursive_factorial

print(recursive_factorial(5))
print(iterative_factorial(5))